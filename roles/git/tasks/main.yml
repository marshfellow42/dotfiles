---
- name: "{{ role_name }} | Checking for Distribution Config: {{ ansible_facts['distribution'] }}"
  ansible.builtin.stat:
    path: "{{ role_path }}/tasks/{{ ansible_facts['distribution'] }}.yml"
  register: distribution_config


- name: "{{ role_name }} | Distribution specific tasks + Git setup"
  when: distribution_config.stat.exists
  block:

    - name: "{{ role_name }} | Run Tasks: {{ ansible_facts['distribution'] }}"
      ansible.builtin.include_tasks: "{{ ansible_facts['distribution'] }}.yml"

    ############################################################
    # Git configuration
    ############################################################

    - name: "{{ role_name }} | Configure credential.helper"
      community.general.git_config:
        name: credential.helper
        scope: global
        value: store

    ############################################################
    # User
    ############################################################

    - name: "{{ role_name }} | Set user.name"
      community.general.git_config:
        name: user.name
        scope: global
        value: "{{ git_user_name }}"

    - name: "{{ role_name }} | Set user.email"
      community.general.git_config:
        name: user.email
        scope: global
        value: "{{ git_user_email }}"
      when: git_user_email is defined
      no_log: true

    - name: "{{ role_name }} | Set user.signingkey"
      community.general.git_config:
        name: user.signingkey
        scope: global
        value: "{{ ansible_facts['user_dir'] }}/.ssh/id_ed25519.pub"

    ############################################################
    # Commit / Tag signing
    ############################################################

    - name: "{{ role_name }} | Enable commit.gpgsign"
      community.general.git_config:
        name: commit.gpgsign
        scope: global
        value: true

    - name: "{{ role_name }} | Enable tag.gpgsign"
      community.general.git_config:
        name: tag.gpgsign
        scope: global
        value: true

    ############################################################
    # GPG (Linux version)
    ############################################################

    - name: "{{ role_name }} | Set gpg.program"
      community.general.git_config:
        name: gpg.program
        scope: global
        value: gpg

    ############################################################
    # Core
    ############################################################

    - name: "{{ role_name }} | Set core.editor"
      community.general.git_config:
        name: core.editor
        scope: global
        value: nano

    - name: "{{ role_name }} | Set core.autocrlf"
      community.general.git_config:
        name: core.autocrlf
        scope: global
        value: input

    ############################################################
    # Push
    ############################################################

    - name: "{{ role_name }} | Set push.autoSetupRemote"
      community.general.git_config:
        name: push.autoSetupRemote
        scope: global
        value: true

    ############################################################
    # Git LFS
    ############################################################

    - name: "{{ role_name }} | Configure filter.lfs.clean"
      community.general.git_config:
        name: filter.lfs.clean
        scope: global
        value: "git-lfs clean -- %f"

    - name: "{{ role_name }} | Configure filter.lfs.smudge"
      community.general.git_config:
        name: filter.lfs.smudge
        scope: global
        value: "git-lfs smudge -- %f"

    - name: "{{ role_name }} | Configure filter.lfs.process"
      community.general.git_config:
        name: filter.lfs.process
        scope: global
        value: "git-lfs filter-process"

    - name: "{{ role_name }} | Configure filter.lfs.required"
      community.general.git_config:
        name: filter.lfs.required
        scope: global
        value: true