---
- name: "{{ role_name }} | Checking for Distribution Config: {{ ansible_facts['distribution'] }}"
  ansible.builtin.stat:
    path: "{{ role_path }}/tasks/{{ ansible_facts['distribution'] }}.yml"
  register: distribution_config

- name: "{{ role_name }} | Distribution specific tasks + VSCodium setup"
  when: distribution_config.stat.exists
  block:

    - name: "{{ role_name }} | Run Tasks: {{ ansible_facts['distribution'] }}"
      ansible.builtin.include_tasks: "{{ ansible_facts['distribution'] }}.yml"

    - name: "{{ role_name }} | Ensure rmpc config file is symlinked"
      ansible.builtin.file:
        src: "{{ _files_path }}/config.ron"
        dest: "{{ ansible_facts['user_dir'] }}/.config/rmpc/config.ron"
        state: link
        force: yes

    - name: "{{ role_name }} | Ensure rmpc theme file is symlinked"
      ansible.builtin.file:
        src: "{{ _files_path }}/def.ron"
        dest: "{{ ansible_facts['user_dir'] }}/.config/rmpc/themes/def.ron"
        state: link
        force: yes