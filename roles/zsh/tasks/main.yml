---
- name: "{{ role_name }} | Checking for Distribution Config: {{ ansible_facts['distribution'] }}"
  ansible.builtin.stat:
    path: "{{ role_path }}/tasks/{{ ansible_facts['distribution'] }}.yml"
  register: distribution_config

- name: "{{ role_name }} | Distribution specific tasks + {{ role_name }} setup"
  when: distribution_config.stat.exists
  block:

    - name: "{{ role_name }} | Run Tasks: {{ ansible_facts['distribution'] }}"
      ansible.builtin.include_tasks: "{{ ansible_facts['distribution'] }}.yml"

    - name: "{{ role_name }} | Ensure the user has a zsh shell"
      user:
        name: "{{ ansible_facts['user_id'] }}"
        shell: /bin/zsh
        state: present
      become: true

    - name: "{{ role_name }} | Install oh-my-zsh"
      ansible.builtin.command: sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

    - name: "{{ role_name }} | Install zsh-autosuggestions"
      ansible.builtin.git:
        repo: https://github.com/zsh-users/zsh-autosuggestions.git
        dest: "{{ ansible_facts['user_dir'] }}/.oh-my-zsh/custom/plugins/zsh-autosuggestions"

    - name: "{{ role_name }} | Symlink config file"
      ansible.builtin.file:
        src: "{{ _files_path }}/.zshrc"
        dest: "{{ ansible_facts['user_dir'] }}/.zshrc"
        state: link
        force: yes