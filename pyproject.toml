[project]
name = "dotfiles-manager"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=15.0.0",
    "typer>=0.24.1",
]

[project.scripts]
dotfiles = "main:main"

[dependency-groups]
dev = [
    "ruff>=0.15.11",
]
