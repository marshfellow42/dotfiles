[project]
name = "dotfiles-manager"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=15.0.0",
    "rich-argparse>=1.7.2",
    "typer>=0.24.1",
]

[project.scripts]
dotfiles = "main:main"
